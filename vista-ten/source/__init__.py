"""Vista: ten layered backgrounds that loop, on one palette convention.

    python -m projects.vista.build              # all ten, layers and preview
    python -m projects.vista.build --only cairn
    python -m projects.vista.build --free       # just the three-scene taster

The pack exists to be USED, not looked at, and that fixes almost every decision
in it. See `pack.py` for the ones that are not obvious.
"""
